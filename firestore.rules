rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Enable full read and write access during development for testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
